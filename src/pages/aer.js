        // const gridState = this.state.grid;
        var tetrominoMirror = [...this.state.grid]
        var temp 
        // console.log("das", this.props.tetrominoMirror)
        // console.log("HELLO3", tetrominoMirror);

        console.log("JAJA1", aer);
        console.log("JAJA2", er);
        console.log("JAJA3", tetrominoMirror);
        // tetrominoMirror = tetrominoMirror.slice().sort((a, b) => b.stats.speed - a.stats.speed)
        // if (this.props.tetrominoMirror === null) {
        //     this.props.setTetrominoMirror(tetrominoMirror);
        //     console.log("HELLO1", tetrominoMirror);
        // } else {
            // this.props.setTetrominoMirror(aer);
            // this.props.setTetrominoMirror(er);

        //     console.log("HELLO2", this.state.grid);
        // }

        // this.props.setTetrominoMirror(cleanGrid);

            // this.devices = devicesLoaded.map(device => {return {...device};});
            // function replaceItemAtIndex(arr, index, newValue) {  return [...arr.slice(0, index), newValue, ...arr.slice(index + 1)]; }

            // this.props.setTetrominoMirror(tetrominoMirror);
        // } 
        
        // else {
        // // const clone = { 
        //     temp = tetrominoMirror
        //     tetrominoMirror = this.props.tetrominoMirror
        //     // this.props.tetrominoMirror = temp
        //     this.props.setTetrominoMirror(temp);
        // }


        //     grid: Object.assign({}, ...tetrominoMirror) 
        //    };
        //    this.store.dispatch(new Actions.LoginUser(clone));

        // setInterval(() => {
        //     this.props.setTetrominoMirror(null);
        // }, 1000);